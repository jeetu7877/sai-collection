# Base URL of the deployed backend API, including /api.
# Example once deployed on Render: https://menstyle-pro-api.onrender.com/api
# Leave unset for local dev — Vite's proxy (vite.config.js) handles /api locally.
VITE_API_URL=
